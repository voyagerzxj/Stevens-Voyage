// topojson-client v3.1.0  ISC License  https://github.com/topojson/topojson-client
!function(global,factory){
  typeof exports==='object'&&typeof module!=='undefined'?factory(exports):
  typeof define==='function'&&define.amd?define(['exports'],factory):
  (global=global||self,factory(global.topojson={}));
}(this,function(exports){'use strict';

function identity(x){return x;}

function transform(topology){
  if(!topology.transform)return identity;
  var t=topology.transform,x0,y0,
      kx=t.scale[0],ky=t.scale[1],
      dx=t.translate[0],dy=t.translate[1];
  return function(input,i){
    if(!i)x0=y0=0;
    var j=2,n=input.length,output=new Array(n);
    output[0]=(x0+=input[0])*kx+dx;
    output[1]=(y0+=input[1])*ky+dy;
    while(j<n)output[j]=input[j],++j;
    return output;
  };
}

function reverse(array,n){
  var t,j=array.length,i=j-n;
  while(i<--j)t=array[i],array[i++]=array[j],array[j]=t;
}

function feature(topology,o){
  if(typeof o==='string')o=topology.objects[o];
  return o.type==='GeometryCollection'
    ?{type:'FeatureCollection',features:o.geometries.map(function(o){return feature1(topology,o);})}
    :feature1(topology,o);
}

function feature1(topology,o){
  var id=o.id,bbox=o.bbox,
      properties=o.properties==null?{}:o.properties,
      geometry=object(topology,o);
  return id==null&&bbox==null
    ?{type:'Feature',properties:properties,geometry:geometry}
    :bbox==null
    ?{type:'Feature',id:id,properties:properties,geometry:geometry}
    :{type:'Feature',id:id,bbox:bbox,properties:properties,geometry:geometry};
}

function object(topology,o){
  var transformPoint=transform(topology),arcs=topology.arcs;

  function arc(i,points){
    if(points.length)points.pop();
    for(var a=arcs[i<0?~i:i],k=0,n=a.length;k<n;++k)
      points.push(transformPoint(a[k],k));
    if(i<0)reverse(points,n);
  }

  function point(p){return transformPoint(p,0);}

  function line(arcs){
    var points=[];
    for(var i=0,n=arcs.length;i<n;++i)arc(arcs[i],points);
    if(points.length<2)points.push(points[0]);
    return points;
  }

  function ring(arcs){
    var points=line(arcs);
    while(points.length<4)points.push(points[0]);
    return points;
  }

  function polygon(arcs){return arcs.map(ring);}

  function geometry(o){
    var t=o.type,c;
    switch(t){
      case 'GeometryCollection':return{type:t,geometries:o.geometries.map(geometry)};
      case 'Point':c=point(o.coordinates);break;
      case 'MultiPoint':c=o.coordinates.map(point);break;
      case 'LineString':c=line(o.arcs);break;
      case 'MultiLineString':c=o.arcs.map(line);break;
      case 'Polygon':c=polygon(o.arcs);break;
      case 'MultiPolygon':c=o.arcs.map(polygon);break;
      default:return null;
    }
    return{type:t,coordinates:c};
  }

  return geometry(o);
}

exports.feature=feature;
Object.defineProperty(exports,'__esModule',{value:true});
});
